# Metadata Providers API

Metadata providers give rules access to semantic information computed once
per file. See the {doc}`/guide/metadata-providers` guide for usage examples
and the {doc}`/api/semantic` page for the Rust-powered `SemanticModel` API.

```{eval-rst}
.. automodule:: rude.providers
   :members:
   :show-inheritance:
```
