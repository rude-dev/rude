## Summary

<!-- What does this PR do? Link to related issues with "Closes #N". -->

## Test plan

- [ ] Tests pass (`uv run pytest tests/ -x -q`)
- [ ] Lints pass (`uv run ruff check python tests plugins`)
- [ ] Types pass (`uv run mypy`)
